name: Build and Push to Docker Hub

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check Out Code
        uses: actions/checkout@v2
      
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Cache Docker Layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-north-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/travel_backend -f backend/Dockerfile .

      - name: Tag Docker Image
        run: docker tag ${{ secrets.DOCKER_USERNAME }}/travel_backend:latest public.ecr.aws/c3z2n6l2/${{ secrets.DOCKER_USERNAME }}/travel_backend:latest

      # - name: Run Docker
      #   run: docker run -d -p 8000:8000 ${{ secrets.DOCKER_USERNAME }}/travel_backend

      - name: Push Docker image
        run: docker push public.ecr.aws/c3z2n6l2/${{ secrets.DOCKER_USERNAME }}/travel_backend:latest


   
